# Delivery Artifacts — Requirements

Role: owns non-npm artifact flows that ship with LiveStore releases — the
prebuilt DevTools artifact consumed from `overeng`, and the vendored
wa-sqlite build.

## Context

Builds on the parent [requirements.md](../requirements.md). No `LS.DEL-*`
requirements were re-homed here (the former flat node expressed this
contract in its spec plus
[decision 0002](../.decisions/0002-devtools-artifact-cadence.md));
requirement candidates await an alignment round. Companion runbook (owned by
this node): [wa-sqlite-management.md](./wa-sqlite-management.md).
