--- vite.config.ts
+++ vite.config.ts
@@ -8,7 +8,6 @@
 export default defineConfig({
   server: {
     port: process.env.PORT ? Number(process.env.PORT) : 60_001,
-    fs: { strict: false },
   },
   worker: { format: 'es' },
   plugins: [
