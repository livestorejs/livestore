#!/usr/bin/env bash

# In CI, direnv should fail on errors; locally it can be lenient
CI=${CI:-0}
if [ "${CI}" = "1" ] || [ "${CI}" = "true" ] || [ "${CI}" = "yes" ]; then
  strict_env
fi

use devenv
