# Generated file - DO NOT EDIT
# Source: health-release-credentials.yml.genie.ts

name: 'Health: release credentials'

on:
  schedule:
    - cron: 0 7 * * 1
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  verify-credentials:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    defaults:
      run:
        shell: bash
    steps:
      - name: Verify Netlify credentials
        if: ${{ !cancelled() }}
        env:
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
        run: |
          set -euo pipefail
          : "${NETLIFY_AUTH_TOKEN:?Missing NETLIFY_AUTH_TOKEN — the release job needs it}"

          status=$(curl --silent --show-error --max-time 30 --output /dev/null --write-out '%{http_code}' --header "Authorization: Bearer $NETLIFY_AUTH_TOKEN" "https://api.netlify.com/api/v1/user")
          if [ "$status" != "200" ]; then
            echo "::error::credential rejected (HTTP $status)"
            exit 1
          fi
          echo "credential accepted (HTTP $status)"
      - name: Verify Cloudflare credentials
        if: ${{ !cancelled() }}
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          set -euo pipefail
          : "${CLOUDFLARE_API_TOKEN:?Missing CLOUDFLARE_API_TOKEN — the release job needs it}"
          : "${CLOUDFLARE_ACCOUNT_ID:?Missing CLOUDFLARE_ACCOUNT_ID — the release job needs it}"

          response=$(curl --silent --show-error --max-time 30 \
            --header "Authorization: Bearer $CLOUDFLARE_API_TOKEN" \
            https://api.cloudflare.com/client/v4/user/tokens/verify)
          token_status=$(printf '%s' "$response" | jq -r '.result.status // empty')
          if [ "$token_status" != "active" ]; then
            echo "::error::Cloudflare token status is '${token_status:-unknown}', expected 'active'"
            exit 1
          fi
          account_status=$(curl --silent --show-error --max-time 30 --output /dev/null --write-out '%{http_code}' \
            --header "Authorization: Bearer $CLOUDFLARE_API_TOKEN" \
            "https://api.cloudflare.com/client/v4/accounts/$CLOUDFLARE_ACCOUNT_ID")
          if [ "$account_status" != "200" ]; then
            echo "::error::CLOUDFLARE_ACCOUNT_ID not readable with this token (HTTP $account_status)"
            exit 1
          fi
          echo "credential accepted (token active, account readable)"
      - name: Verify Mixedbread credentials
        if: ${{ !cancelled() }}
        env:
          MXBAI_API_KEY: ${{ secrets.MXBAI_API_KEY }}
          MXBAI_VECTOR_STORE_ID_PROD: ${{ secrets.MXBAI_VECTOR_STORE_ID_PROD }}
        run: |
          set -euo pipefail
          : "${MXBAI_API_KEY:?Missing MXBAI_API_KEY — the release job needs it}"
          : "${MXBAI_VECTOR_STORE_ID_PROD:?Missing MXBAI_VECTOR_STORE_ID_PROD — the release job needs it}"

          status=$(curl --silent --show-error --max-time 30 --output /dev/null --write-out '%{http_code}' --header "Authorization: Bearer $MXBAI_API_KEY" "https://api.mixedbread.com/v1/vector_stores/$MXBAI_VECTOR_STORE_ID_PROD")
          if [ "$status" != "200" ]; then
            echo "::error::credential rejected (HTTP $status)"
            exit 1
          fi
          echo "credential accepted (HTTP $status)"
