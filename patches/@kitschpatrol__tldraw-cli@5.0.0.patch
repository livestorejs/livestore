diff --git a/dist/lib/index.js b/dist/lib/index.js
index 6fbd0385d40f27c773ef5da5d7b8f433a008674a..f9665b6cb62aea76f39c38df241692a4f3b139e1 100644
--- a/dist/lib/index.js
+++ b/dist/lib/index.js
@@ -30855,6 +30855,7 @@ var log_default = log;
 var LocalTldrawServer = class {
   constructor(tldrData) {
     this.tldrData = tldrData;
+    this.connections = /* @__PURE__ */ new Set();
   }
   get href() {
     if (!this.server) throw new Error("Server not started");
@@ -30863,9 +30864,28 @@ var LocalTldrawServer = class {
   }
   port;
   server;
-  close() {
+  connections;
+  async close() {
     if (!this.server) throw new Error("Server not started");
-    this.server.close();
+    const server = this.server;
+    this.server = void 0;
+    this.port = void 0;
+    server.closeAllConnections?.();
+    server.closeIdleConnections?.();
+    await new Promise((resolve, reject) => {
+      server.close((error) => {
+        if (error) {
+          reject(error);
+          return;
+        }
+        resolve();
+      });
+    });
+    for (const socket of this.connections) {
+      socket.destroy();
+    }
+    this.connections.clear();
+    server.unref?.();
     log_default.info("Stopped tldraw server");
   }
   async start() {
@@ -30890,9 +30910,22 @@ var LocalTldrawServer = class {
         fetch: app.fetch,
         port: this.port
       });
+      this.server.unref?.();
+      this.server.on("connection", (socket) => {
+        this.connections.add(socket);
+        socket.on("close", () => {
+          this.connections.delete(socket);
+        });
+      });
       await this.waitForServer();
     } catch (error) {
       log_default.error(error);
+      if (this.server) {
+        try {
+          await this.close();
+        } catch {
+        }
+      }
       throw error;
     }
     log_default.info(`tldraw hosted at "${this.href}"`);
@@ -47810,12 +47843,27 @@ async function tldrawToImage(tldrPathOrUrl, options = {}) {
   if (isLocal) await tldrawServer.start();
   const tldrawUrl = isLocal ? tldrawServer.href : validatedPathOrUrl.href;
   const tldrawController = new TldrawController(tldrawUrl);
-  await tldrawController.start();
-  const exportReport = await tldrawController.download(options);
-  await tldrawController.close();
-  if (isLocal) tldrawServer.close();
-  log_default.info(`Export time: ${prettyMilliseconds(performance.now() - startTime)}`);
-  return exportReport;
+  let controllerStarted = false;
+  try {
+    await tldrawController.start();
+    controllerStarted = true;
+    const exportReport = await tldrawController.download(options);
+    log_default.info(`Export time: ${prettyMilliseconds(performance.now() - startTime)}`);
+    return exportReport;
+  } finally {
+    if (controllerStarted) {
+      try {
+        await tldrawController.close();
+      } catch {
+      }
+    }
+    if (isLocal) {
+      try {
+        await tldrawServer.close();
+      } catch {
+      }
+    }
+  }
 }
 function sanitizeName(name, format) {
   const extension = path5.extname(name);
