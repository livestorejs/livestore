--- metro.config.js
+++ metro.config.js
@@ -13,7 +13,7 @@
 
 // Needed for monorepo setup (can be removed in standalone projects)
 const projectRoot = __dirname
-const monorepoRoot = path.resolve(projectRoot, '../..')
+const monorepoRoot = path.resolve(projectRoot, '../../../..')
 
 config.watchFolders = [monorepoRoot]
 
@@ -22,7 +22,16 @@
   path.resolve(monorepoRoot, 'node_modules'),
 ]
 
-addLiveStoreDevtoolsMiddleware(config, { schemaPath: './src/schema/index.ts' })
+addLiveStoreDevtoolsMiddleware(config, {
+  schemaPath: './src/schema/index.ts',
+  viteConfig: (viteConfig) => {
+    viteConfig.server.fs ??= {}
+    // Point to Overtone monorepo root
+    viteConfig.server.fs.allow = [process.env.WORKSPACE_ROOT + '/../..']
+    viteConfig.optimizeDeps.force = true
+    return viteConfig
+  },
+})
 
 // console.log(config)
 module.exports = config
