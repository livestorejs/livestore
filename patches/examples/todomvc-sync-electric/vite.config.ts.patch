--- vite.config.ts
+++ vite.config.ts
@@ -26,6 +26,9 @@
   server: {
     port: process.env.PORT ? Number(process.env.PORT) : 60_001,
     headers: credentiallessHeaders,
+    fs: {
+      strict: false,
+    },
   },
   preview: {
     headers: credentiallessHeaders,
