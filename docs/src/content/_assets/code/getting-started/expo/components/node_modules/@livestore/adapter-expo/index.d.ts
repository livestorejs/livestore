export type MakeDbOptions = {
  sync?: unknown
  storage?: { subDirectory?: string }
  clientId?: string
  sessionId?: string
}

export declare function makePersistedAdapter(options?: MakeDbOptions): any
