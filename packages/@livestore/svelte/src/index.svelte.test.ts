import { describe, expect, it } from 'vitest'

describe('test', () => {
  // TODO pending design review
  it('should pass', () => {
    expect(true).toBe(true)
  })

  it('should fail', () => {
    expect(false).toBe(false)
  })
})
