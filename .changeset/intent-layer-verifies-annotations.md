---
---

No release impact. Adds `Verifies:` requirement-traceability comments to existing test files in `@livestore/common` and `@livestore/react` as part of establishing the intent layer (VRS tree) under `context/`. No shipped behavior changes.
